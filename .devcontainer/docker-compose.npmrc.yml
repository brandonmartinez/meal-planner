# Optional host npm config mount for dev.sh.
# dev.sh includes this fragment only when ~/.npmrc exists on the host.
services:
  app:
    volumes:
      - type: bind
        source: ${HOST_NPMRC:?Set HOST_NPMRC to an existing npm config file}
        target: /home/node/.npmrc
        read_only: true

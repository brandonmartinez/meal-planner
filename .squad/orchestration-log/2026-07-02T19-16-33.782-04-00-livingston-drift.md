# Orchestration Log: Livingston — CI drift gate hotfix (pg_trgm GIN index declaration)

**Agent:** Livingston (Backend)  
**Task:** Fix main branch protection + CI drift gate (pg_trgm schema declaration)  
**Wave:** 6 (Sprint 2 unblocking, critical path)  
**Mode:** urgent (schema-only fix, no migration, blocks all subsequent PRs)

**Why:** main was RED on the CI drift gate due to #111's raw-SQL pg_trgm GIN index. Index exists in the DB but was NOT declared in schema.prisma, causing `prisma migrate diff --exit-code` to report drift → gate FAILED on every PR. Blocking all Wave 6 + later merges.

**Root cause:** #111 created `Meal_name_trgm_idx` via raw SQL (deliberate, to avoid postgresqlExtensions preview per #94). But raw-SQL-only approach is untenable under branch protection — the drift gate forces schema declarativity.

**Fix produced:**
- `packages/api/prisma/schema.prisma` — ONLY file changed:
  - `generator client`: added `previewFeatures = ["postgresqlExtensions"]`
  - `datasource db`: added `extensions = [pg_trgm]`
  - `Meal` model: declared `@@index([name(ops: raw("gin_trgm_ops"))], map: "Meal_name_trgm_idx", type: Gin)`

**Decision:** Reverses #94's choice to avoid postgresqlExtensions preview. Justified by @brandonmartinez: drift gate makes declarativity non-negotiable under branch protection.

**Deviation from spec:** Coordinator's task specified `extensions = [pgTrgm]`. Changed to `[pg_trgm]` (lowercase). Rationale: Prisma v6.19.3 emits identifiers LITERALLY. `[pgTrgm]` → `CREATE EXTENSION IF NOT EXISTS "pgTrgm"` (wrong). `[pg_trgm]` → `CREATE EXTENSION IF NOT EXISTS "pg_trgm"` (matches DB catalog). Verified via offline `prisma migrate diff --from-empty` proof.

**Verification (READ-ONLY):**
- Isolated build: pnpm install (frozen)
- `prisma validate` → valid ✓
- `prisma generate` → client v6.19.3 built ✓
- Offline drift proof: schema-derived SQL = migration.sql byte-for-byte ✓
- api tsc → GREEN ✓
- api vitest → 472 passed ✓

**Impact:**
- main branch protection lifted to GREEN
- Wave 6 PRs (#135 linus-108, #136 saul-100) now unblocked
- Downstream: #97 rebase now conflict-safe (adjacent-line schema edit + new columns)

**Status:** Merged (inline hotfix to main, not a separate PR)

**Outcome:** ✓ Complete. CI drift gate fixed. main GREEN. All subsequent Wave 6 merges unblocked. No regressions; schema is now fully declarative and matches DB state.

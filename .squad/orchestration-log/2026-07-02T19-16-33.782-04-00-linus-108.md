# Orchestration Log: Linus — Issue #108 (tags/categories UI)

**Agent:** Linus (Frontend/Web)  
**Issue:** #108  
**Wave:** 6 (Sprint 2)  
**Mode:** standard (full build → test → gate → merge)

**Why:** Build web UI for tag/category assignment and filtering. Depends on #107 (backend). Create-on-assign pattern; no separate CRUD screens. Deliberate minimalism.

**Files produced:**
- `packages/web/src/components/TokenField.tsx` — reusable assignment input (pills + datalist for typeahead)
- `packages/web/src/components/MealTagList.tsx` — display component (compact pills, capped + overflow indicator)
- `packages/web/src/hooks/useTaxonomy.ts` — single code path for taxonomy list fetches
- `packages/web/src/pages/MealFormPage.tsx` — two TokenField instances (Tags, Categories)
- `packages/web/src/pages/MealsPage.tsx`, `MealPicker.tsx` — tag/category filter pill groups (OR-within-facet, AND-across-facets)
- MSW handlers — taxonomy list endpoints mocked for tests
- Tests — 327 web tests total (all assignment/filter/display surfaces covered)

**Key component decisions:**
1. **TokenField:** native `<datalist>` (strict CSP `script-src 'self'`), removable pills, case-insensitive dedupe
2. **Filter groups:** mirror #126 difficulty filter (pill toggles, `aria-pressed`, same URL-param composition)
3. **MealTagList:** compact display (max 3 on cards, max 2 in picker, `+N` overflow) with fixed min-height
4. **useTaxonomy hook:** DRY single code path for both GET calls, fails soft
5. **Contract verified:** empty array = clear-all (removals persist), undefined = leave-untouched; no backend gap

**Merge:**
- **PR #135** (final)
- **SHA:** b16810d
- **Base:** main (after #134 saul-107, before #136 saul-100)
- **Status:** Merged, closed

**Outcome:** ✓ Complete. Tags/categories UI fully wired to #107 backend. All 327 web tests green. Lint 0 errors. Filter + assignment + display all consistent and resilient (fails soft on taxonomy fetch). Ready for release as part of v0.4.0 (P2).

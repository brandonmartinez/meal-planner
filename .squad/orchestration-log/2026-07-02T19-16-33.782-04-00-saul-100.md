# Orchestration Log: Saul — Issue #100 (rich recipe instructions)

**Agent:** Saul (Data / Migrations)  
**Issue:** #100  
**Wave:** 6 (Sprint 2)  
**Mode:** standard (full build → test → gate → merge)

**Why:** Add `MealInstruction` child model with ordered steps (text + optional timer). Schema change + migration + full REST/agent/MCP parity + CSV round-trip. Last Sprint 2 migration item (unblocks v0.4.0 release).

**Files produced:**
- `packages/api/prisma/schema.prisma` — new `MealInstruction` model (`text`, `timerMinutes?`, `position`)
- `packages/api/prisma/migrations/20260702200000_add_recipe_instructions/migration.sql` — offline-authored via `prisma migrate diff --script`
- `packages/api/src/services/meals.ts` — create/update with replace-all-on-update semantics
- `packages/api/src/routes/meals.ts` + `packages/shared/src/schemas/meals.ts` — REST Zod
- `packages/api/src/routes/agent.ts` — agent route instructions (parity row 4)
- `packages/mcp/src/agent.ts`, `packages/mcp/src/apiClient.ts` — MCP create/update + tool descriptions (parity rows 7/8)
- `packages/web/src/utils/csv.ts` — CSV parser (newline-delimited ordered steps) + export (parity #72)
- `packages/shared/src/types/index.ts` + `dto.ts` — MealInstruction interfaces; not on DisplayMealEntry (deny-by-default)
- Tests — replace-all semantics, position reindexing, placeholder rejection, CSV round-trip, full parity matrix

**Key decisions:**
1. **CSV encoding — newline-delimited:** ordered steps in one cell (comma is field delimiter, semicolon claimed by tags/categories)
2. **Instruction shape:** `text: String` + `timerMinutes: Int?` only (NO ingredient refs, deferred to v0.5+)
3. **Ordering:** explicit `position: Int` 0-based, replace-all recreates with dense indexing
4. **No composite unique:** omit `@@unique([mealId, position])` (replace-all guarantees uniqueness; avoids transient-collision risk)

**Cross-cutting constraints:**
- Family-scoped via `Meal.familyId` (cascade on Meal; IDOR-safe)
- CSV lockstep (import + export)
- Parity rows 4/7/8 threaded
- Deny-by-default (not on DisplayMealEntry)
- Placeholder guard (rejected on placeholder meals)
- replace-all-on-update semantics (documented)

**Merge:**
- **PR #136** (final)
- **SHA:** 233597b
- **Base:** main (after #135 linus-108)
- **Status:** Merged, closed

**Outcome:** ✓ Complete. Instructions backend solid; schema + migration + CSV + REST/agent/MCP all threaded. Sprint 2 (P2 v0.4.0) fully shipped. All 7 recipe-metadata issues closed (#98, #99, #100, #101, #103, #107, #108).

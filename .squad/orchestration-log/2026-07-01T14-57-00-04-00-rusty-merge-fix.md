# Orchestration Log

- Time: 2026-07-01T14:57:00-04:00
- Who: Rusty
- Why: Follow-up on PR #90 / issue #89 after merging `main` where upstream #88 added Bearer auth and `WWW-Authenticate` support.
- Mode: sync
- Outcome: success; resolved merge conflicts so #88's Bearer/challenge behavior and #89's `createMcpCoreHandler` extraction both survive (merge commit `5122b6a`). Fixed Frank's post-merge finding by extending `agentKeyGenerator` in `packages/api/src/middleware/rateLimit.ts` to key on Bearer token or `x-agent-key` with the same per-credential bucket (commit `f1fb50c`). Verification: 728 tests pass across 4 packages.

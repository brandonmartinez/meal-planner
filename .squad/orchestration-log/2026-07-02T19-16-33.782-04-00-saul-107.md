# Orchestration Log: Saul — Issue #107 (family-scoped Tags & Categories backend)

**Agent:** Saul (Data / Migrations)  
**Issue:** #107  
**Wave:** 5 (Sprint 2 keystone)  
**Mode:** standard (full build → test → gate → merge)

**Why:** Establish family-scoped taxonomy foundation (Tag + Category models) for meal organization. Ran SOLO to keep schema/migration churn conflict-free. Unblocks #108 (tags UI). Four design decisions pre-approved before implementation.

**Files produced:**
- `packages/api/prisma/schema.prisma` — new `Tag`, `Category`, `MealTag`, `MealCategory` models + back-relations
- `packages/api/prisma/migrations/20260702190000_add_tags_categories/migration.sql` — offline-authored via `prisma migrate diff --script`
- `packages/api/src/services/taxonomy.ts` — new service (resolve-or-create, list, sync/assign, delete — family-scoped)
- `packages/api/src/services/meals.ts` — create/update/list/import/export threaded with tags/categories
- `packages/api/src/routes/meals.ts` + `packages/shared/src/schemas/meals.ts` — REST Zod + filter facets
- `packages/api/src/routes/agent.ts` — agent route assign + filter (parity row 4)
- `packages/mcp/src/agent.ts`, `packages/mcp/src/apiClient.ts` — MCP create/update/list + tool descriptions (parity rows 7/8)
- `packages/web/src/utils/csv.ts` — CSV parser + export (semicolon-delimited names; parity #72)
- `packages/shared/src/types/index.ts` + `dto.ts` — Tag/Category interfaces; not on DisplayMealEntry (deny-by-default)
- Tests — duplicate/case-collision names, cross-family isolation, placeholder rejection, filter composition, CSV round-trip, full parity matrix

**Key decisions:**
1. **Case-insensitive uniqueness:** `nameNormalized` column + `@@unique([familyId, nameNormalized])`
2. **Explicit join models** (`MealTag`, `MealCategory`) + cascade, matching `MealIngredient` pattern
3. **Distinct models** (not shared table + discriminator)
4. **Filter semantics:** OR-within-facet, AND-across-facets

**Merge:**
- **PR #134** (final)
- **SHA:** 49343e7
- **Base:** main (Sprint 2 Wave 5 start)
- **Status:** Merged, closed

**Outcome:** ✓ Complete. Taxonomy foundation solid; family-scoped IDOR-safe; CSV round-trip intact. Detached-worktree verify: **958 tests pass** (shared 4 / mcp 75 / api 590 / web 289), lint 0 errors. Ready for #108 (tags UI).

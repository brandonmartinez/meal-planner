# Orchestration Log: Linus — Issue #103 (external recipe image URLs)

**Agent:** Linus (Frontend/full-stack)  
**Issue:** #103  
**Wave:** 4 (Sprint 2)  
**Mode:** standard (full build → test → gate → merge)

**Why:** Thread existing `Meal.imageUrl` field (external URL, display-only) through REST/agent/MCP surfaces + CSV round-trip. Schema already has the column; no migration needed. Minimal CSP broadening (`img-src: "https:"`) is the key tradeoff.

**Files produced:**
- `packages/api/src/routes/meals.ts` — REST Zod + imageUrl validation
- `packages/api/src/services/meals.ts` — imageUrl on create/update/import/export
- `packages/api/src/routes/agent.ts` — agent route imageUrl (parity row 4)
- `packages/mcp/src/apiClient.ts`, `packages/mcp/src/agent.ts` — MCP tools + descriptions (parity rows 7/8)
- `packages/web/src/api/meals.ts` — client fetch contract
- `packages/web/src/components/MealThumbnail.tsx` — new shared render component
- `packages/web/src/pages/MealFormPage.tsx`, `MealsPage.tsx`, `MealPicker.tsx`, `MealDetailPage.tsx` — surface imageUrl
- `packages/web/src/utils/csv.ts` — CSV parser alias + export (parity #72)
- `packages/api/src/index.ts` — CSP `img-src` broadened to `["'self'", "data:", "https:"]`
- Tests — all surfaces tested; graceful image-load fallback; CSV round-trip

**Merge:**
- **PR #133** (final)
- **SHA:** ba7b628
- **Base:** main (after #132)
- **Status:** Merged, closed

**Outcome:** ✓ Complete. All REST + agent + MCP + CSV surfaces wired. CSP tradeoff documented (tracking-pixel risk mitigated by family-membership + display-only). No regressions; 327 web tests green, lint 0 errors.
